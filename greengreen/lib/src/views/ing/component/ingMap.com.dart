import 'package:flutter/material.dart';

class IngMapComponent extends StatelessWidget {
  const IngMapComponent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
